Edit README, CHANGES, Makefile (suppress -warn-errors)
Edit version.ml
commit everything et make a github release, with proper name (vX.YY)
sh release/mktag.sh > release/tag.sh
sh release/release.sh

Change wepage for non-dev versions
